import { AuthLayout } from '../../components/Auth';
import { AuthFormForget } from '../../components/Auth/AuthFormForget';

export const AuthForgetScreen = () => {
  return (
    <AuthLayout>
      <AuthFormForget />
    </AuthLayout>
  );
};
