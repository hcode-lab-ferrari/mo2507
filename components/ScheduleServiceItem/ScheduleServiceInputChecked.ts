import styled from "styled-components/native";
import { vars } from "../../values";

export const ScheduleServiceInputChecked = styled.View`
    width: 14px;
    height: 14px;
    background-color: ${vars.blue};
`;