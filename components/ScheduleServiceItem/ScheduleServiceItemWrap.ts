import styled from "styled-components/native";
import { vars } from "../../values";

export const ScheduleServiceItemWrap = styled.TouchableOpacity`
  flex-direction: row;
  margin-bottom: ${vars.spacePx};
`;