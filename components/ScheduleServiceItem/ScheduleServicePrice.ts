import styled from "styled-components/native";
import { vars } from "../../values";

export const ScheduleServicePrice = styled.Text`
    color: ${vars.dark0};
    font-size: 16px;
    font-weight: bold;
`;