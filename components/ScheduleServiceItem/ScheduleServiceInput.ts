import styled from "styled-components/native";
import { vars } from "../../values";

export const ScheduleServiceInput = styled.View`
    width: 22px;
    height: 22px;
    margin-right: ${vars.spacePx};
    border: ${vars.gray3} 1px solid;
    justify-content: center;
    align-items: center;
`;