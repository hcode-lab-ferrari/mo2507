import styled from 'styled-components/native';
import { vars } from '../../values';

export const FooterIcons = styled.View`
  flex-direction: row;
  align-items: center;
  margin: ${vars.spacePx} 0;
`;
