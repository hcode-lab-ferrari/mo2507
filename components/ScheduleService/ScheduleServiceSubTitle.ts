import styled from 'styled-components/native';
import { vars } from '../../values';

export const ScheduleServiceSubTitle = styled.Text`
  font-size: 20px;
  color: ${vars.gray2};
  text-transform: uppercase;
`;
