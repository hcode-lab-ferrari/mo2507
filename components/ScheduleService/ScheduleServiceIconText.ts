import styled from 'styled-components/native';
import { vars } from '../../values';

export const ScheduleServiceIconText = styled.Text`
  font-size: 16px;
  font-weight: bold;
  color: ${vars.gray0};
  margin-top: ${vars.spacePx};
`;
