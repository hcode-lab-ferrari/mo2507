import styled from 'styled-components/native';
import { vars } from '../../values';

export const ScheduleServiceIcon = styled.View`
  align-items: center;
  justify-content: center;
  margin: ${vars.spacePx};
`;
