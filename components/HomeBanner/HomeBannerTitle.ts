import styled from 'styled-components/native';
import { vars } from '../../values';

export const HomeBannerTitle = styled.Text`
  color: ${vars.white};
  font-size: 80px;
  font-weight: bold;
  text-align: right;
`;
