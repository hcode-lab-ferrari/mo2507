import styled from 'styled-components/native';
import { vars } from '../../values';

export const PageHeaderSubTitle = styled.Text`
  font-size: 14px;
  font-weight: 500;
  color: ${vars.white};
  text-transform: uppercase;
  text-align: center;
`;
