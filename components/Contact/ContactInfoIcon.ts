import styled from 'styled-components/native';
import { vars } from '../../values';

export const ContactInfoIcon = styled.View`
  position: absolute;
  top: -50px;
  background-color: ${vars.white};
  min-width: 100px;
  justify-content: center;
  align-items: center;
`;
