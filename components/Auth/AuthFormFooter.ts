import styled from 'styled-components/native';
import { vars } from '../../values';

export const AuthFormFooter = styled.View`
  flex-direction: row;
  justify-content: space-between;
  min-height: 50px;
  margin-top: ${vars.spacePx};
`;
