import styled from 'styled-components/native';
import { vars } from '../../values';

export const AuthForgetText = styled.Text`
  font-size: 14px;
  color: ${vars.dark0};
  margin-left: ${vars.space / 2}px;
`;
