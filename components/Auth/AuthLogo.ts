import styled from 'styled-components/native';
import { vars } from '../../values';

export const AuthLogo = styled.Image`
  width: 100px;
  resize-mode: contain;
  margin-top: ${vars.space * 3 * -1}px;
`;
